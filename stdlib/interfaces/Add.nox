interface Add<T, R> {
    add(a: Self, b: T): R
}


implement Add for Fraction {
    func add(a: Fraction, b: Fraction): Fraction {
        return ...
    }

    func add(a: Fraction, b: Int): Fraction {
        return ...
    }

    func add(a: Int, b: Fraction): Fraction {
        return ...
    }
}
