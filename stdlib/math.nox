import { foldl } from core
import { floor } from _internal


export floor
export min = foldl(?, (x, y) => x < y ? x : y)
export max = foldl(?, (x, y) => x > y ? x : y)
export sum = foldl(?, (x, y) => x + y, 0)
